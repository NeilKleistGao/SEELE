set(LIGHT_PATH_PREFIX light)

set(LIGHT_HEADERS
        ${LIGHT_PATH_PREFIX}/light.h
        ${LIGHT_PATH_PREFIX}/point_light.h
        ${LIGHT_PATH_PREFIX}/light_manager.h)

set(LIGHT_SRCS
        ${LIGHT_PATH_PREFIX}/point_light.cc
        ${LIGHT_PATH_PREFIX}/light_manager.cc)